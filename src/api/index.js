const { Router } = require("express");
/**
 * Import all of your routes here like this
 *
 * const randomRoute = require("./routes/whateverroute")
 *
 */

module.exports = () => {
  const app = Router();
  // randomRoute(app);
  return app;
};
